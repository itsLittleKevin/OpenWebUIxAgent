# Environment variables for Open WebUI backend
# Copy to .env and modify as needed. The launch script loads this automatically.

# --- Whisper STT ---
WHISPER_VAD_FILTER=true
WHISPER_MULTILINGUAL=true
WHISPER_COMPUTE_TYPE=int8
# WHISPER_LANGUAGE=         # Leave empty for auto-detect, or set to e.g. "en", "zh"
# WHISPER_MODEL_DIR=        # Default: ~/.cache/whisper/models

# --- CORS (for dev mode) ---
CORS_ALLOW_ORIGIN=http://localhost:5173;http://localhost:8080

# --- Ollama ---
# OLLAMA_BASE_URL=http://localhost:11434

# --- OpenAI-compatible API (for cloud LLMs) ---
# OPENAI_API_BASE_URL=https://api.openai.com/v1
# OPENAI_API_KEY=

# --- TTS (when using GPT-SoVITS via OpenAI-compatible endpoint) ---
# AUDIO_TTS_OPENAI_API_BASE_URL=http://localhost:9880/v1
# AUDIO_TTS_OPENAI_API_KEY=none
