# Open WebUI Custom Tools

Python tool/function scripts to be loaded into Open WebUI via Admin > Functions/Tools.

- `vmc_expression_filter.py` — Filter function: sentiment → VRM expressions via VMC protocol
- `deep_memory_search.py` — Tool: search across all conversation history (keyword + semantic)
- `reminders.py` — Tool: set/list/check reminders with due dates
